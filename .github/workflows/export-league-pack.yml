name: Export League Pack

on:
  workflow_dispatch:
    inputs:
      league:
        description: "API-Football league id"
        required: true
        default: "39"
      season:
        description: "Season starting year, e.g. 2025 for 2025/26"
        required: true
        default: "2025"
      players:
        description: "Optional derived players snapshot path"
        required: false
        default: ""
      clubs:
        description: "Optional derived clubs snapshot path"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  export-pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Derive players for requested season
        run: |
          if [ -n "${{ github.event.inputs.players }}" ]; then
            echo "Explicit derived players snapshot supplied."
          else
            npm run derive:players -- --league=${{ github.event.inputs.league }} --season=${{ github.event.inputs.season }} --leagueTier=S
          fi
      - name: Derive clubs for requested season
        run: |
          if [ -n "${{ github.event.inputs.clubs }}" ]; then
            echo "Explicit derived clubs snapshot supplied."
          else
            npm run derive:clubs -- --league=${{ github.event.inputs.league }} --season=${{ github.event.inputs.season }}
          fi
      - name: Export league pack
        run: |
          COMMAND="npm run export:league-pack -- --league=${{ github.event.inputs.league }} --season=${{ github.event.inputs.season }}"
          if [ -n "${{ github.event.inputs.players }}" ]; then
            COMMAND="$COMMAND --players='${{ github.event.inputs.players }}'"
          fi
          if [ -n "${{ github.event.inputs.clubs }}" ]; then
            COMMAND="$COMMAND --clubs='${{ github.event.inputs.clubs }}'"
          fi
          eval "$COMMAND"
      - name: Commit league pack and derived data
        run: |
          if [ -z "$(git status --porcelain derived exports/league-packs)" ]; then
            echo "No data changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add derived exports/league-packs
          git commit -m "Export league pack"
          git push
